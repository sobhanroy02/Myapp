# Suppress peer dependency warnings for cleaner builds
legacy-peer-deps=true

# Use exact versions for better reproducibility
save-exact=true

# Disable funding messages
fund=false

# Disable audit suggestions during install
audit=false